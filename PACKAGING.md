# 打包说明

## 收录

1. Gemini原始PDF按字节复制；按页文本为已有提取副本；世界观整理稿按来源保存。
2. 《归乡疫期》原始稿独立提取；合作稿从文学文本操作恢复；用户与Claude的可见会话另存。
3. 两套架构分别保存原始用户消息与排版整理版。后者修复表格列和代码块格式，不将设计说成实现。
4. 本次工作稿完整复制，当前v0.2和旧v0.1分开；工作稿中的本机文件链接改为包内相对链接。原始来源路径仍可出现在出处说明中。
5. PLM、LightRAG和酒馆只附参考说明、少量原文档快照及原许可证。

## 排除与转换

未上传Claude完整导出的隐藏推理、工具检索带入的其他小说正文、无关会话、旧Word二进制、运行数据库、凭据、模型权重、环境或整个参考软件仓库。完整Claude导出文件在本机保留，其指纹记录在来源文件中。

恢复合作稿时保留了历史失败与无效替换记录，没有为了凑篇幅自行补写。恢复结果可由 scripts/rebuild_claude_manuscript.py 重新计算；该脚本只处理字符串，不执行历史命令。

sources/design-messages 与第三方文档快照按原样保留，内部可能仍有原环境链接。主体导航、小说文件、两套设计和本轮工作稿都可以独立阅读。

## 验证

运行 python3 scripts/verify_bundle.py，检查文件集合、大小、SHA-256、参考快照以及合作稿重建一致性。

MANIFEST.json 覆盖除自身以外的所有有效文件，避免自引用。发布前另生成仓库外完整索引清单，包含MANIFEST.json本身，用git-evolution-workflow的索引校验器核对暂存内容。

归档检验不代表参考软件已部署、不代表历史小说事实核验完成，也不代表新试稿已经获得用户采纳。
